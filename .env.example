# Copy to .env.local. Never put secrets in NEXT_PUBLIC_* variables.
# The default app uses clearly labeled synthetic fixtures. No funds move.
ASSAY_MODE=demo
JUPITER_API_KEY=
PYTH_API_KEY=
SOLANA_RPC_URL=
# ASSAY_MODE=live needs JUPITER_API_KEY and SOLANA_RPC_URL; it compares real quotes
# for the verified AAPL wrappers, read-only. PYTH_API_KEY is optional and not yet used.

